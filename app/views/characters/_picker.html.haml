-# View partials
  scene: Scene that characters can be picked from
  selected_char: Character, can be nil, depending on if a character is already selected

- selected_char ||= nil

.mb-4
  - if selected_char.present?
    Reading as
    %strong= selected_char.name
    = link_to 'Change character', scene_path(@scene), class: 'underline'
  - else
    Read as:
    - scene.characters.each do |char|
      = link_to char.name, scene_path(@scene, params: { character_id: char.id }), class: 'underline'
